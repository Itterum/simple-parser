services:
  worker:
    build:
      context: ./services/worker
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

  backend:
    build:
      context: .
      dockerfile: services/backend/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
    depends_on:
      - worker
    restart: always
